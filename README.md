# Pipedream Netsuite Connector

A collection of custom Pipedream actions for working with the NetSuite API.

## Available Actions
#### Initalize - Initialize NetSuite with credentials and return config object
#### HTTP Request - Run a generic NetSuite HTTP request.
#### Query Records - Run a SuiteQL query against NetSuite records.

